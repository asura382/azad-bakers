# Azad Bakers final setup

Use only `supabase/complete-migration.sql` in Supabase SQL Editor.

Optional: `supabase/seed-demo.sql` is demo data only.

Customer: http://localhost:5500/public/
Owner: http://localhost:5500/admin/

Run from the `azad-bakers-v2` folder:
`python -m http.server 5500`

The supplied reference storefront design is preserved; its menu/order code has been replaced with the newer Supabase-backed implementation.
